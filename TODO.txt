Add percent of reads mapping back to the contigs.

Add the annotation in terms of metabolism. How many can be annotated (without what they are actually).

Add some taxonomic annotation (scale with number of reads mapping back to contig). The broad groups, bacteria, viruses etc. Any cyanobacteria ?